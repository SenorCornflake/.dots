[bar/main]
wm-restack = bspwm
width = 100%
height = 30

background = ${c.alpha_background}
foreground = ${c.alpha_foreground}

bottom = true

modules-left = title
modules-center = desktops
modules-right = volume wifi battery time

font-0 = Dina:size=10;2

[bar/eDP-1]
inherit = bar/main
monitor = eDP-1

[bar/VGA-1]
inherit = bar/main
monitor = VGA-1

[module/desktops]
type = internal/bspwm

format = <label-state>

ws-icon-0 = 1;WEB
ws-icon-1 = 2;DEV
ws-icon-2 = 3;TERM
ws-icon-3 = 4;FILES
ws-icon-4 = 5;5
ws-icon-5 = 6;6
ws-icon-6 = 7;7
ws-icon-7 = 8;8
ws-icon-8 = 9;9
ws-icon-9 = 10;10
ws-icon-default = _

label-focused  = < %icon% >
label-dimmed = %icon%
label-occupied = %icon%
label-urgent   = ! %icon% !
label-empty    = _

label-focused-padding  = 1
label-dimmed-padding   = 1
label-occupied-padding = 1
label-urgent-padding   = 1
label-empty-padding    = 1

label-focused-foreground  = ${c.alpha_label}
label-dimmed-foreground   = ${c.alpha_foreground}
label-occupied-foreground = ${c.alpha_foreground}
label-urgent-foreground   = ${c.alpha_foreground}
label-empty-foreground    = ${c.alpha_foreground}

[module/title]
type = internal/xwindow
format = <label>

format-prefix = "[ "
format-suffix = " ]"

format-prefix-foreground = ${c.alpha_text}
format-suffix-foreground = ${c.alpha_text}
format-padding = 1
label-maxlen = 40
label = %title% 
format-empty = <label>
label-empty = _
label-foreground = ${c.alpha_foreground}

[module/time]
type = internal/date
format-prefix = "TIME: "
format-prefix-foreground = ${c.alpha_label}
format = <label>
format-padding = 1
time = %A, %d | %H:%M
label = %time%
label-foreground = ${c.alpha_text}

[module/volume]
type = internal/pulseaudio
sink = alsa_output.pci-0000_00_1b.0.analog-stereo
use-ui-max = true
interval = 5
format-volume = <label-volume>
format-muted = <label-muted>
format-volume-prefix = "VOL: "
format-volume-prefix-foreground = ${c.alpha_label}
format-muted-prefix-foreground = ${c.alpha_label}
format-muted-prefix = "VOL: "
format-volume-padding = 1
format-muted-padding = 1
label-volume = %percentage%
label-volume-foreground = ${c.alpha_text}
label-muted = x
label-muted-foreground = ${c.alpha_urgent}

[module/battery]
type = internal/battery
battery = BAT0
ac = AC

format-full        = <label-full>
format-discharging = <label-discharging>
format-charging    = <label-charging>

format-full-prefix        = "BAT: "
format-discharging-prefix = "BAT: "
format-charging-prefix    = "BAT: "

format-full-padding        = 1
format-discharging-padding = 1
format-charging-padding    = 1

format-full-prefix-foreground        = ${c.alpha_label}
format-discharging-prefix-foreground = ${c.alpha_label}
format-charging-prefix-foreground    = ${c.alpha_label}

label-charging    = %percentage%
label-full        = %percentage%
label-discharging = %percentage%

label-charging-foreground    = ${c.alpha_text}
label-full-foreground    = ${c.alpha_text}
label-discharging-foreground    = ${c.alpha_text}

[module/wifi]
type = internal/network
interface = wlan0
interval = 1
accumulate-stats = true
unknown-as-up = true

format-connected = <label-connected>
format-disconnected = <label-disconnected>

format-connected-padding = 1
format-disconnected-padding = 1

format-connected-prefix = "NET: "
format-disconnected-prefix = "NET: "

format-connected-prefix-foreground = ${c.alpha_label}
format-disconnected-prefix-foreground = ${c.alpha_label}

label-connected = %essid% %downspeed% %upspeed%
label-disconnected = x

label-connected-foreground = ${c.alpha_text}
label-disconnected-foreground = ${c.alpha_urgent}
